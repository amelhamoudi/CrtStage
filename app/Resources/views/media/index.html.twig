{% extends 'base.html.twig' %}

{% set title="Media" %}

{% block css %}
{% endblock css %}
{% block content %}
<div class="box">
    <div class="box-body">


        <table class="table table-striped table-bordered table-hover" id="datatable">
        <thead>
            <tr>
                <th>Id</th>
                <th>Image name</th>
                <th>Updated at</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for media in media %}
            <tr>
                <td><a href="{{ path('media_show', { 'id': media.id }) }}">{{ media.id }}</a></td>
                <td><img src="{{ asset('uploads/media/' ~ media.imageName) }}" width="90"></td>
                <td>{% if media.updatedAt %}{{ media.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>

                    <div class="btn-group">
                        <a href="{{ path('media_show', { 'id': media.id }) }}" class="btn btn-info"><i class="fa fa-search"></i></a>
                        <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown">
                            <span class="caret"></span>
                            <span class="sr-only">Toggle Dropdown</span>
                        </button>

                        <ul class="dropdown-menu" role="menu">
                            <li>
                                <a href="{{ path('media_edit', { 'id': media.id }) }}">modifier</a>
                            </li>
                        </ul>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

            <a href="{{ path('media_new') }}">Créer</a>
    </div>
</div>
{% endblock %}
{% block js %}
{% endblock %}
