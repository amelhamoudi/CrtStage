{% extends 'base.html.twig' %}
{% set title="Ajouter une nouvelle media" %}
{% block css %}

{% endblock %}
{% block content %}
<div class="box">
    <div class="box-body">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('media_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>/ </div>
{% endblock %}
