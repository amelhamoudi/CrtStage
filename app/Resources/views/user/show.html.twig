{% extends 'base.html.twig' %}
{% set title="Afficher Administrateur" %}
{% block css %}
{% endblock %}
{% block content %}
    <div class="box">
        <div class="box-body">


            <table class="table table-striped table-bordered table-hover" id="myTable">


                <tbody>
                <tr>
                    <th>N°</th>
                    <td>{{ user.id }}</td>
                <tr>
                    <th>Nom </th>
                    <td> {{ user.username }} </td>
                </tr>
                <tr>
                    <th> Email </th>
                    <td> {{ user.email }} </td>
                </tr>
                <tr>
                    <th> Role </th>
                    <td> {{ user.role }} </td>
                </tr>

                </tbody>
            </table>


            <a href="{{ path('user_edit', { 'id': user.id }) }}" class="btn btn-success">Modifier</a>


        </div>
    </div>
{% endblock %}
